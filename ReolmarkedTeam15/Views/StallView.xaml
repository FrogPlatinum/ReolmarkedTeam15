<Page x:Class="ReolmarkedTeam15.Views.StallView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ReolmarkedTeam15.Views"
      mc:Ignorable="d" 
      d:DesignHeight="900" d:DesignWidth="1000"
      FontSize="14"
      Title="StallView">

    <Grid Background="#793327">
        <!-- ^ Anden farve:#6b7140 -->

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Liste overskrift-->
        <Label Grid.Column="0" Grid.Row="0" Content="Liste Her!" Foreground="White"/>
        
        <!-- Liste i Datagrid-->
        <DataGrid Grid.Column="0" Grid.Row="1" Background="White" ItemsSource="{Binding Stalls}" Margin="0,0,0,0" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Placering" Binding="{Binding Location }" Width="4*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Indretning" Binding="{Binding StallType }" Width="4*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Udlejningsstatus" Binding="{Binding Availability}" Width="6*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Lejes Af" Binding="{Binding StallRenterID }" Width="4*" IsReadOnly="True"/>
                
            </DataGrid.Columns>

        </DataGrid>
        
        <!--Knapper i et stackpanel-->
        <StackPanel Grid.Column="1" Grid.Row="2" Width="80" Margin="10,0,10,0">
            <Button Content="Udlej Reol" HorizontalAlignment="Center" Width="80" FontWeight="SemiBold" Margin="0,0,0,10"
                    Click="RentOutStall_Click"/>
        <!--Jeg tænker det giver mening også at have disse to knapper :)--> 
            <Button Content="Gør Ledig" HorizontalAlignment="Center" Width="80" FontWeight="SemiBold" Margin="0,0,0,10"></Button>
            <Button Content="Rediger" HorizontalAlignment="Center" Width="80" FontWeight="SemiBold" ></Button>
        </StackPanel>
        
        <!-- Oversigt overskrift-->
        <Label Grid.Column="2" Grid.Row="0" Content="Oversigt!" Margin="10,0,0,0" Foreground="White"/>
        
        <!-- Oversigt via png billede-->
        <Image Grid.Column="2" Grid.Row="1" Source="/Views/StallLayout.png" Stretch="UniformToFill" Margin="0,0,0,0" />

    </Grid>
</Page>
